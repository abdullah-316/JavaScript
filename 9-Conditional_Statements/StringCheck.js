

let str = 'amma';

if (str[0] === 'a' && str.length > 3) {
    console.log("Good String");
}
else{
    console.log("Not a Good String");
    
}

// Question: A string is a golden string if it starts with the character ‘A’ or ‘a’ and has a total length greater than 5. For a given string, print if it is golden or not.

let character = "Aesthetic";

if ((character[0] === 'A' || 'a' ) &&  character.length > 5 ) {
    console.log("Golden String"); 
}
else{
    console.log("Not a Golden String");
}